Django>=5.1
djangorestframework>=3.15
django-cors-headers>=4.4

# -------- 读取 .env 配置 --------
django-environ>=0.11

# -------- Redis 客户端（CACHES / SESSION / Celery 等都会用到） --------
redis>=5.0

# -------- 服务器监控（CPU/内存/磁盘/网络指标采集） --------
psutil>=5.9

# -------- 定时任务调度器（CRON / 固定间隔 / 指定时间触发） --------
apscheduler>=3.10,<4

# -------- MySQL 驱动 (Windows 下建议用: pip install mysqlclient，有预编译 wheel) --------
# mysqlclient>=2.2

# -------- PostgreSQL 驱动 (psycopg3, Django 5.1 自动优先使用; 中文 Windows 下 psycopg2 解码 libpq 消息会崩) --------
psycopg[binary]>=3.2
